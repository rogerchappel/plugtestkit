# Template notes

plugtestkit templates are generated from `src/templates.js` so they can receive plugin metadata and matrix values. This directory is shipped to make that packaging choice explicit and to reserve space for future static template assets.
